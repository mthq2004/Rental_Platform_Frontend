export type StepKey = 1 | 2 | 3 | 4;

export type BrandPalette = {
  primary: string;
  primarySoft: string;
  text: string;
  muted: string;
  border: string;
  panel: string;
  background: string;
};
